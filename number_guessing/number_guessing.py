#importing the random module (default python) --- could be useful for protein data! if its a database like thing lol
import random

#last integer will not be considered, first will; (-1, 11) is really -1 - 10
r = random.randrange(-1, 11)
print(r)
